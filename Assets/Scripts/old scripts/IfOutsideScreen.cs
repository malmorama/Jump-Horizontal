using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IfOutsideScreen : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        /*
        //if gameobject outside screen respawn
        if (gameObject.transform.position.x <= (Camera.main.transform.position.x - 45f))
        {

            Destroy(gameObject);
        }
        */
    }

}
